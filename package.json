{
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "scripts": {
    "start": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\" ",
    "heroku-postbuild" : "npm install && cd frontend && npm install && cd ../backend && npm install && cd .. && npm run start"
  }
}
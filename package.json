{
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "scripts": {
    "start": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\" ",
    "postinstall": "./install.sh",
    "heroku-postbuild": "npm run postinstall && npm run start"
  }
}

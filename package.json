{
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "scripts": {
    "start": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\" ",
    "install": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "heroku-postbuild": "npm run install && npm start"
  }
}
